# Release Notes

## v0.1.0

- Launch `silitop` as a focused Apple Silicon fork of `asitop`
- Fix incorrect E-core and P-core cluster utilization reporting
- Add regression tests for parser behavior
- Introduce `silitop` package namespace and CLI entrypoint
- Preserve a compatibility `asitop` command during migration
- Refresh project branding, README, and package metadata
